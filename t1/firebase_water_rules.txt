{
  "rules": {
    "$id": {
      "access": {
        ".validate": "newData.isString()"
      },
      "address": {
        ".validate": "newData.isString()"
      },
      "city": {
        ".validate": "newData.isString()"
      },
      "description": {
        ".validate": "newData.isString()"
      },
      "filtration": {
        ".validate": "newData.isString()"
      },
      "gp_id": {
        ".validate": "newData.isString()"
      },
      "handicap": {
        ".validate": "newData.isString()"
      },
      "hours": {
        ".validate": "newData.hasChildren(['close', 'open'])",
        "close": {
          ".validate": "newData.hasChildren(['day', 'time'])",
          "day": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 6"
          },
          "time": {
            ".validate": "newData.isString() && newData.val().matches(/^(0[0-9]|1[0-9]|2[0-3])[0-5][0-9]$/)"
          },
          "$other": {
            ".validate": false
          }
        },
        "open": {
          ".validate": "newData.hasChildren(['day', 'time'])",
          "day": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 6"
          },
          "time": {
            ".validate": "newData.isString() && newData.val().matches(/^(0[0-9]|1[0-9]|2[0-3])[0-5][0-9]$/)"
          },
          "$other": {
            ".validate": false
          }
        },
        "$other": {
          ".validate": false
        }
      },
      "lat": {
        ".validate": "newData.isNumber()"
      },
      "lon": {
        ".validate": "newData.isNumber()"
      },
      "norms_rules": {
        ".validate": "newData.isString()"
      },
      "organization": {
        ".validate": "newData.isString()"
      },
      "permanently_closed": {
        ".validate": "newData.isBoolean()"
      },
      "phone": {
        ".validate": "newData.isString()"
      },
      "quality": {
        ".validate": "newData.isString()"
      },
      "service": {
        ".validate": "newData.isString()"
      },
      "statement": {
        ".validate": "newData.isString()"
      },
      "status": {
        ".validate": "newData.isString()"
      },
      "tap_type": {
        ".validate": "newData.isString()"
      },
      "tapnum": {
        ".validate": "newData.isNumber()"
      },
      "vessel": {
        ".validate": "newData.isString()"
      },
      "zip_code": {
        ".validate": "newData.isString()"
      }
    }
  }
}
